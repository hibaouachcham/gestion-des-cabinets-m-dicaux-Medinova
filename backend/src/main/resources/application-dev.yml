# Configuration de développement pour MySQL Workbench
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medinova?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Hiba123@#
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

logging:
  level:
    com.medinova: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

